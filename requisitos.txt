# Dependencias para el proyecto Grabador Fuente MPD - KEY:KID

# Para la versión GUI en Windows
customtkinter

# Para la versión consola en Android (Termux)
psutil

# Notas:
# - En Windows: pip install -r requisitos.txt
# - En Android/Termux: Instala Python con 'pkg install python', luego 'pip install psutil'
# - El binario N_m3u8DL-RE (versión Linux/Android) debe descargarse por separado desde GitHub y colocarse en el PATH o directorio del script.
# - Asegúrate de tener .NET runtime instalado en Termux si es necesario: 'pkg install dotnet-runtime'
